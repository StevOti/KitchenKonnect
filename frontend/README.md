# KitchenKonnect Frontend

Scaffolded Vite + React frontend. To run locally:

```powershell
cd frontend
npm install
npm run dev
```

The app provides initial pages: `/`, `/login`, `/register` that connect to backend auth endpoints.
